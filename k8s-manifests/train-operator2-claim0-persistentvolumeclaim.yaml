apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: train-operator2-claim0
  name: train-operator2-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
